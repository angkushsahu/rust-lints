# Rust lints

Git hooks for linting rust (pre-commit and commit-msg)

Go to the `.git` directory and inside the `hooks` directory, create two files - `commit-msg` and `pre-commit`

In each of these files, paste the contents from the corresponding files in this repository.
